name: Node.js CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
  
    runs-on: ubuntu-latest

    steps:
      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Deploy to Raspberry Pi and start server
        run: |
          echo "Namlas@786" | sshpass -p - ssh salman@59.182.0.211 'cd ~/my-node-app && git pull origin main && npm install && node index.js'
